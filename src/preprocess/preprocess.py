import pandas as pd
import datetime

"""
TODO: 1. Clean the data
        - remove special characters
        - drop rows that are 80% empty
        - remove duplicate rows
        - make sure dates are consistent
        - normalize categorical values (e.g., "NM", "Near Mint", "near-mint" → "Near Mint")
        - look for outliers via statistical methods to remove them
        - ensure correct data types
TODO: 2. Vectorize/encode the data
TODO: 3. Feature engineering
"""
